{% extends 'base.html.twig' %}

{% block title %}Datos{% endblock %}

{% block body %}
    <h1>Datos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dato.id }}</td>
            </tr>
            <tr>
                <th>WindSpeed</th>
                <td>{{ dato.WindSpeed }}</td>
            </tr>
            <tr>
                <th>SolarIrradiance</th>
                <td>{{ dato.SolarIrradiance }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ dato.Location }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_datos_index') }}">back to list</a>

    <a href="{{ path('app_datos_edit', {'id': dato.id}) }}">edit</a>

    {{ include('datos/_delete_form.html.twig') }}
{% endblock %}
